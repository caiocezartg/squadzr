## Agent skills

### Issue tracker

Issues are tracked in the Linear `squadzr` workspace under the Squadzr team (`SQU`) using the Linear CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five canonical triage labels together with the applicable project-area labels. See `docs/agents/triage-labels.md`.

### Domain docs

This repository uses a multi-context layout for client, server, and shared packages. See `docs/agents/domain.md`.
